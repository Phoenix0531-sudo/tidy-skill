interface:
  display_name: "洁癖.skill"
  short_description: "Keep local agent environments clean"
  default_prompt: "Use $tidy-skill to audit this local agent environment and recommend safe cleanup boundaries."
